#include <iostream>

int main() {
  int data[] = {20, 30, 50, 80, 90};
  int size = 5;
  int low = 0;
  int high = size - 1;
  int midle  = size / 2;
  while (i != size) {

    int h = size;
  }
  return 0;
}